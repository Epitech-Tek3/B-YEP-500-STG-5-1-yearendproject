export const discordListen = async (accessToken: string, action: string, reaction: string) => {
  console.log("discord -> accessToken: " + accessToken);
  console.log("aciton: " + action);
  console.log("reaciton: " + reaction);
}
