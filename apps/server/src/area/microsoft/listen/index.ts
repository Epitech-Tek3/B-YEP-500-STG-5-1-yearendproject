export const microsoftListen = async (accessToken: string, action: string, reaction: string) => {
  console.log("microsoft -> accessToken: " + accessToken);
  console.log("aciton: " + action);
  console.log("reaciton: " + reaction);
}
