export const spotifyListen = async (accessToken: string, action: string, reaction: string) => {
  console.log("spotify -> accessToken: " + accessToken);
  console.log("aciton: " + action);
  console.log("reaciton: " + reaction);
}
